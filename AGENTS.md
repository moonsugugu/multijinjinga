# MoonsuneZip 홈서버 앱 개발 기본 규칙

이 저장소의 앱이 사용자 계정, 수업, 활동 결과, 점수, 거래, 게시물처럼 **데이터가 누적되는 기능**을 제공한다면 아래 규칙을 기본값으로 적용한다.

## 1. 기본 아키텍처

- 정적 화면과 프런트엔드는 GitHub에 저장한다.
- 운영 데이터와 실시간 통신은 `moonsunezip.com` 홈서버를 사용한다.
- 운영 API 기본 주소는 `https://api.moonsunezip.com`이다.
- 운영 데이터베이스는 홈서버의 PostgreSQL이다.
- 브라우저에서 PostgreSQL에 직접 접속하지 않는다. 반드시 HTTPS API를 통한다.
- 외부에서 PostgreSQL의 5432 포트를 열거나 DB 접속정보를 프런트엔드에 넣지 않는다.
- Firebase를 사용해야 한다면 Google 로그인 등 **인증 전용**으로만 사용한다. 누적 앱 데이터는 Firestore나 Firebase Realtime Database에 저장하지 않는다.
- 이미지·영상·대용량 파일은 PostgreSQL에 바이너리로 저장하지 않는다. 파일 저장소에 저장하고 DB에는 URL과 메타데이터만 저장한다.

## 2. 개발 및 배포 흐름

- 다른 컴퓨터에서는 앱 코드를 작성하고 테스트한 뒤 GitHub `main` 브랜치에 푸시하는 것까지만 수행한다.
- 홈서버 배포, PostgreSQL 실행, Cloudflare Tunnel, 백업은 서버 노트북이 담당한다.
- 서버 노트북의 비밀번호, DB 접속 문자열, Cloudflare 토큰을 GitHub에 올리지 않는다.
- `.env`, `.env.local`, 인증 JSON, 백업 파일, `*.before-*`는 반드시 `.gitignore`에 포함한다.
- API 변경이 필요한 앱은 프런트 코드만 임의로 가정하지 말고, 기존 홈서버 API 규격으로 구현하거나 서버 측 변경 필요 사항을 문서에 명시한다.

## 3. 데이터 경로 규칙

- 앱마다 고유하고 변경되지 않는 영문 소문자 `APP_ID`를 정한다.
- 서로 다른 앱의 데이터가 섞이지 않도록 모든 문서 경로를 앱 단위로 구분한다.
- 권장 경로 예시:
  - `apps/{APP_ID}`
  - `apps/{APP_ID}/classes/{classId}`
  - `apps/{APP_ID}/classes/{classId}/students/{studentId}`
- 학급형 앱은 모든 실시간 구독과 갱신 범위를 `classId` 또는 `roomId` 단위로 제한한다.
- 전체 DB나 다른 반의 변경사항을 모든 사용자에게 방송하지 않는다.

## 4. 동시접속 최적화 필수 규칙

- 모든 저장 버튼은 클릭 즉시 비활성화하고 처리 완료 후 다시 활성화한다.
- 같은 버튼의 연속 클릭, 더블클릭, 모바일 터치 중복 입력을 막는다.
- 모든 쓰기 요청에 `crypto.randomUUID()`로 만든 `requestId`를 포함한다.
- 네트워크 재시도 시에는 새로운 ID를 만들지 말고 **같은 requestId**를 재사용한다.
- 돈, 재고, 좌석, 주식, 포인트처럼 정확성이 필요한 변경은 PostgreSQL 트랜잭션 또는 서버 원자 연산을 사용한다.
- 클라이언트가 읽은 잔액을 단순 덮어쓰는 방식으로 구현하지 않는다.
- 충돌 응답 `409`가 오면 짧은 랜덤 지연 후 제한된 횟수만 재시도한다.
- 서버 혼잡 응답 `503`이 오면 즉시 반복 호출하지 말고 1~5초 지수 백오프를 적용한다.
- 입력 자동저장은 매 키 입력마다 전송하지 말고 300~800ms 디바운스를 적용한다.
- 실시간 화면 갱신은 이벤트마다 전체 목록을 다시 그리지 말고 50~300ms 동안 모아 한 번에 반영한다.
- 자주 바뀌지 않는 읽기 데이터는 브라우저 캐시나 Cloudflare 캐시를 사용한다.
- 목록은 무제한으로 한 번에 조회하지 말고 페이지네이션 또는 개수 제한을 둔다.
- 연결이 끊기면 지수 백오프로 재연결하되 최대 재연결 간격을 15~30초로 제한한다.

## 5. 서버 혼잡 처리 UX

- 정상 상태에서는 고정 지연을 넣지 않는다.
- 요청이 대기 중이면 버튼에 `처리 중...`을 표시한다.
- 혼잡할 때는 `사용자가 많아 순서대로 처리 중입니다`라고 안내한다.
- 5초 이상 걸리면 실패로 단정하지 말고 취소 또는 계속 기다리기 선택지를 제공한다.
- 처리 결과가 불명확할 때 같은 거래를 새 requestId로 다시 보내지 않는다. 먼저 기존 요청 결과를 조회한다.
- 낙관적 UI를 사용하더라도 서버 실패 시 원래 상태로 되돌릴 수 있어야 한다.

## 6. 홈서버 API 기준

- 상태 확인: `GET https://api.moonsunezip.com/health`
- 정상 응답에는 PostgreSQL 상태와 다음 부하 정보가 포함될 수 있다.
  - `load.activeWrites`
  - `load.queuedWrites`
  - `load.writeLatencyMs`
  - `load.recommendedDelayMs`
- 서버의 적응형 대기열이 정상 시 즉시 처리하고 혼잡도에 따라 약 0.2초, 0.75초, 2초, 최대 5초로 쓰기를 분산한다.
- 클라이언트는 서버 대기열을 우회하거나 동일 요청을 병렬로 반복 전송하지 않는다.
- 기존 경제 앱의 Firestore 호환 어댑터 구조는 `class-economy/src/lib/postgres-firestore.js`를 참고한다.
- 기존 반별 실시간 앱 구조는 `multijinjinga/postgres-adapter.v1.js`를 참고한다.

## 7. 보안 및 데이터 보호

- 브라우저가 보내는 사용자 ID, 금액, 권한을 그대로 신뢰하지 않는다.
- 교사 전용 작업과 관리자 작업은 서버에서 인증 및 권한을 확인한다.
- SQL 문자열을 직접 이어 붙이지 말고 파라미터 바인딩을 사용한다.
- API 요청 본문 크기, 문자열 길이, 숫자 범위, 배열 크기를 검증한다.
- 로그에 비밀번호, PIN, 토큰, 쿠키, DB 접속정보를 출력하지 않는다.
- 학생 개인정보는 꼭 필요한 최소 범위만 저장한다.
- 삭제 기능은 관련 하위 데이터 처리와 복구·백업 정책을 함께 고려한다.

## 8. 완료 전 검사

- 프로덕션 빌드가 성공해야 한다.
- 최소 25명의 동시 저장 시나리오에서 누락과 중복이 없어야 한다.
- 동일 requestId를 두 번 보내도 데이터가 한 번만 변경되어야 한다.
- 서로 다른 반의 실시간 이벤트가 섞이지 않아야 한다.
- 연결 종료 후 재연결이 되어야 한다.
- API 지연·409·503·오프라인 상황에서 무한 재시도하지 않아야 한다.
- `.env`와 비밀정보가 Git 변경 목록에 없어야 한다.
- 마지막 작업은 GitHub `main` 푸시까지이며, 홈서버에 원격 명령을 직접 실행하지 않는다.

## 9. AI 코딩 도우미에게 주는 우선 지시

이 파일의 규칙을 프로젝트 기본값으로 따른다. 데이터가 쌓이는 기능을 발견하면 먼저 홈서버 PostgreSQL API 사용을 전제로 설계한다. 동시 사용자 25명 단위의 학급 여러 개가 같은 순간에 사용할 수 있다고 가정한다. 구현 시 정확성, 중복 방지, 반별 격리, 적응형 혼잡 처리, Git 비밀정보 제외를 필수 완료 조건으로 취급한다.
